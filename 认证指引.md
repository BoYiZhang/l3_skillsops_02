# 操作指引
## 0. 时间要求
本次考试限制时间为 4 小时，请务必在4小时内完成提交。

## 1. 准备仓库与分支
### 1.1 所有代码都必须提交到 https://g.hz.netease.com/ 个人仓库中，并且给 EasyDatal3Practice 这个 public deploy key 开启访问权限


### 1.2 操作步骤示例
本地没有代码时，新建一个仓库然后克隆到本地：

git clone ssh://git@g.hz.netease.com:22222/bigdata-skills/l3_practice_sample.git
cd l3_practice_sample
已有本地目录时，将远程指到你的个人专属仓库并推送：

``````
git remote add origin ssh://git@g.hz.netease.com:22222/bigdata-skills/l3_practice_sample.git
git branch -M main
git push -uf origin main
``````

### 1.3 分支规范
在独立功能分支上开发，统一命名为 feature/dev，避免直接在 main 上堆叠未验证修改。
提交信息写清楚「做了什么、为了谁」，便于评审与回溯；尽量小步提交，避免单次超大 diff。

## 2. 开发指引
请结合自身开发经验与 AI 辅助工具，完成题目实现。

技术选型原则
后端语言与框架不限（如 Java、Python、Go、Rust 等均可），建议优先选用所在团队统一规范的语言进行方案落地，更贴合实际业务开发场景
可使用 AI 进行需求拆解、代码生成、测试补全与问题排查
选型需保证可运行、可评审、可复现，不得依赖仅本机可用的隐式环境
工程结构

``````
├── frontend/           # 前端代码
├── backend/            # 后端代码
├── doc/                # 项目文档
├── docker-compose.yml  # Docker MySQL（本地安装 MySQL 则不需要）
└── .gitignore
``````


考生自行在 frontend/ 和 backend/ 中搭建工程、选择框架和组织代码。

##  前端页面基准要求
    必须在 doc/ 中补充主要前端页面截图，建议覆盖核心流程页面。
    未提供该截图说明将影响「功能完整性」维度评估。

## 判题维度参考
功能完整性
技术实现质量
可测试性
设计文档与 Spec Driven
AI 工具链使用
代码可维护性


